import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.languagetranslator',
  appName: 'LanguageTranslator',
  webDir: 'dist'
};

export default config;
